# Tuft webhook agents

Versioned instructions for automated [Tuft webhook agents](https://docs.tuft.dev/#webhooks).

Each top-level directory is intended to be selected as the webhook's optional
subdirectory in the Tuft dashboard. See the directory's README for trigger and
skill configuration.
