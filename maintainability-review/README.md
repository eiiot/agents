# expo/tuft maintainability review

Runs the `maintainability-and-bloat-review` skill for relevant pull-request
events from `expo/tuft` and reports the result through the Tuft webhook run.

## Tuft setup

1. In **MCP & Skills**, import
   `https://github.com/eiiot/skills/tree/main/maintainability-and-bloat-review`.
2. In **Webhooks**, create a webhook using:
   - repository: `eiiot/agents`
   - ref: `main`
   - subdirectory: `maintainability-review`
   - agent: Codex or Claude Code
   - Slack channel: the channel where review reports should appear
3. Install the Tuft GitHub App with read access to `expo/tuft` so `gh` can
   clone the repository and read pull requests.
4. In the `expo/tuft` GitHub repository, add a webhook whose payload URL is the
   private URL generated by Tuft, content type is `application/json`, SSL
   verification is enabled, and event selection is **Pull requests** only.

The full Tuft webhook URL is a secret. Store it only in GitHub's webhook
configuration; never commit or log it. GitHub webhook retries are safe because
the workflow has no GitHub write side effects.

## Supported deliveries

The agent reviews non-draft PRs on `opened`, `reopened`, `synchronize`, and
`ready_for_review`. Other repositories, actions, drafts, malformed bodies, and
stale deliveries end without a notification.

Use GitHub's **Redeliver** action on a pull-request delivery to test the setup.
The agent pins the review to the payload's head SHA and discards its report if
the PR changes while the review is running.
